{% extends 'base.html.twig' %}

{% block title %}Projet{% endblock %}

{% block body %}
    <h1>Projet</h1>

        
        {% for projet in projets %}
            <div class="conteneur-projet">    
                    
                    <div><a href="{{ projet.lien }}"><img class="imgprojet" src="{{ projet.image }}"></a></div>
                        <div class="descriptionprojet">
                            <h1 class="titre">{{ projet.nom }}</h1>
                            {{ projet.description }}
                        </div>
                    
                    {# <td>
                        <a href="{{ path('projet_show', {'id': projet.id}) }}">show</a>
                        <a href="{{ path('projet_edit', {'id': projet.id}) }}">edit</a>
                    </td> #}
                </tr>
            </div>
        {% else %}
        
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <!-- <a href="{{ path('projet_new') }}">Create new</a> -->
{% endblock %}
